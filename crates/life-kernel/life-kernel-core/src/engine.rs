//! In-process [`aios_protocol::ports::KernelPort`] implementation —
//! `KernelEngine` — composes backend registry, gate chain, and metering
//! wrapper.
//!
//! Populated by BRO-870. This ticket (BRO-869) only ships the building
//! blocks consumed there.

/// Placeholder marker so the module compiles until BRO-870 fills it.
pub struct KernelEnginePlaceholder;
