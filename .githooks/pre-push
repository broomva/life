#!/bin/sh
# Pre-push: run smoke tests before pushing
if [ -x ./scripts/control/smoke.sh ]; then
  ./scripts/control/smoke.sh
  exit 0
fi
if command -v make >/dev/null 2>&1 && make -n smoke >/dev/null 2>&1; then
  make smoke
  exit 0
fi
